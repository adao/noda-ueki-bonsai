---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Contact" 
  description="Get in touch with Noda Ueki and Bonsai to discuss your Japanese garden, Ueki, or Bonsai project."
>
  <!-- Contact Section -->
  <section class="section">
    <div class="container">
      <h1>Contact Us</h1>
      
      <!-- Contact Form -->
      <div class="contact-form-container">
        <p>Fill out the form below and we'll get back to you within 1-2 business days.</p>
        
        <form 
          name="contact" 
          method="POST" 
          data-netlify="true" 
          netlify-honeypot="bot-field"
          class="contact-form"
        >
          <!-- Netlify form detection -->
          <input type="hidden" name="form-name" value="contact" />
          
          <!-- Honeypot field for spam prevention -->
          <p class="visually-hidden">
            <label>Don't fill this out if you're human: <input name="bot-field" /></label>
          </p>
          
          <div class="form-group">
            <label for="name">Name *</label>
            <input type="text" id="name" name="name" required />
          </div>
          
          <div class="form-group">
            <label for="email">Email *</label>
            <input type="email" id="email" name="email" required />
          </div>
          
          <div class="form-group">
            <label for="phone">Phone</label>
            <input type="tel" id="phone" name="phone" />
          </div>
          
          <div class="form-group">
            <label for="service">Service Interest</label>
            <select id="service" name="service">
              <option value="">Select a service...</option>
              <option value="ueki">Ueki (Japanese Garden Trees)</option>
              <option value="bonsai">Bonsai Services</option>
              <option value="design">Garden Design</option>
              <option value="maintenance">Maintenance</option>
              <option value="consultation">Consultation</option>
            </select>
          </div>
          
          <div class="form-group">
            <label for="message">Message *</label>
            <textarea id="message" name="message" required placeholder="Tell us about your project..."></textarea>
          </div>
          
          <button type="submit" class="btn btn-primary">Send Message</button>
        </form>
      </div>
      
      <!-- Other Ways to Get in Touch -->
      <div class="contact-info">
        <h2>Other Ways to Get in Touch</h2>
        
        <div class="info-grid">
          <div class="info-item">
            <strong>Email</strong>
            <a href="mailto:uekiandbonsai@yahoo.com">uekiandbonsai@yahoo.com</a>
          </div>
          
          <div class="info-item">
            <strong>Phone</strong>
            <a href="tel:+13105324318">(310) 532-4318</a>
            <span class="note">(call after 5pm)</span>
          </div>
          
          <div class="info-item">
            <strong>Service Area</strong>
            <p>We proudly serve Redondo Beach, Rancho Palos Verdes, Torrance, Gardena, El Segundo, Hawthorne, and surrounding South Bay cities in Los Angeles.</p>
          </div>
        </div>
      </div>
      
      <!-- FAQ Callout -->
      <div class="faq-callout">
        <h3>Have Questions?</h3>
        <p>Check out our <a href="/faq">Frequently Asked Questions</a> for answers to common questions about our services, pricing, and process.</p>
      </div>
    </div>
  </section>
</Layout>

<style>
  .contact-form-container {
    max-width: var(--max-width-form);
    margin-bottom: var(--spacing-xxl);
  }
  
  .contact-form-container > p {
    margin-bottom: var(--spacing-lg);
    color: var(--color-text-light);
  }
  
  .contact-form {
    background-color: var(--color-white);
    padding: var(--spacing-lg);
    border-radius: var(--border-radius);
  }
  
  .form-group {
    margin-bottom: var(--spacing-md);
  }
  
  .contact-info {
    margin-top: var(--spacing-xxl);
  }
  
  .contact-info h2 {
    margin-bottom: var(--spacing-lg);
  }
  
  .info-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-xxl);
  }
  
  .info-item strong {
    display: block;
    margin-bottom: var(--spacing-xs);
    color: var(--color-primary);
    font-size: var(--font-size-small);
  }
  
  .info-item a {
    color: var(--color-text);
  }
  
  .info-item p {
    margin: 0;
  }
  
  .info-item .note {
    display: block;
    font-size: var(--font-size-xs);
    color: var(--color-text-light);
    margin-top: 4px;
  }
  
  /* FAQ Callout */
  .faq-callout {
    margin-top: var(--spacing-xxl);
    padding: var(--spacing-lg);
    background-color: var(--color-white);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  }
  
  .faq-callout h3 {
    margin-bottom: var(--spacing-sm);
    color: var(--color-primary);
  }
  
  .faq-callout p {
    margin: 0;
  }
  
  .faq-callout a {
    color: var(--color-accent);
    font-weight: 500;
  }
  
  @media (max-width: 768px) {
    .contact-form {
      padding: var(--spacing-md);
    }
    
    .info-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }
  }
</style>
